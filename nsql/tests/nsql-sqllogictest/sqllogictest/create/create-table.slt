statement error both default and identity specified for column `x` of table `t`
CREATE TABLE t(x int PRIMARY KEY GENERATED ALWAYS AS IDENTITY DEFAULT 42)

statement ok
CREATE TABLE t(x int PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, y int DEFAULT 42)

statement ok
INSERT INTO t(y) VALUES (41)

statement ok
INSERT INTO t(x) VALUES (5)

statement error violates unique constraint
INSERT INTO t(x) VALUES (1)

query II rowsort
SELECT * FROM t
----
1 41
5 42

statement error multiple primary keys for table `t1` are not allowed
CREATE TABLE t1(x int PRIMARY KEY, y int PRIMARY KEY);


