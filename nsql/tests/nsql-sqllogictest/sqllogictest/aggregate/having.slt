query error
SELECT x, SUM(y) FROM (VALUES (1, 1), (1, 3), (2, 3)) AS t(x, y) GROUP BY x HAVING y = 4
----
expression `t.y` must appear in the GROUP BY clause or be used in an aggregate function

# query I
# SELECT x, SUM(y) FROM (VALUES (1, 1), (1, 3), (2, 3)) AS t(x, y) GROUP BY x HAVING SUM(y) > 3
# ----
# 1 4

