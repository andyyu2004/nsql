statement ok
CREATE TABLE xs(x int PRIMARY KEY)

statement ok
CREATE TABLE ys(y int PRIMARY KEY, z int)

statement ok
INSERT INTO xs VALUES (1), (2), (3)

statement error
INSERT INTO xs(z) VALUES (1)
----
column `z` not in scope


statement error
INSERT INTO xs VALUES (1, 2)
----
table `xs` has 1 columns but 2 columns were supplied

query II rowsort
SELECT x FROM xs
----
1
2
3

statement error
INSERT INTO xs(y) VALUES (1), (2), (3)
----
column `y` not in scope

# should pad `z` with NULL
statement ok
INSERT INTO ys(y) VALUES (1), (2), (3)

query II rowsort
SELECT y, z FROM ys
----
1 NULL
2 NULL
3 NULL

statement error
INSERT INTO xs(x, x) VALUES (1, 2), (2, 3), (3, 4)
----
duplicate column name in insert column list: `x`

statement error
INSERT INTO xs(x) VALUES ('bad')
----
cannot insert value of type `text` into column `x` of type `int`

statement ok
CREATE TABLE zs(x int PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, y int DEFAULT 42)

statement ok
INSERT INTO zs(x) VALUES (1), (2), (3)

query II rowsort
SELECT x, y FROM zs
----
1 42
2 42
3 42

# same should work without an explicit column list
statement ok
INSERT INTO zs VALUES (4), (5)

query II rowsort
SELECT x, y FROM zs
----
1 42
2 42
3 42
4 42
5 42

